import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class HashingService {

  getHashedString(hashMe: string): string {
    return "";
  }


  constructor() { }
}
